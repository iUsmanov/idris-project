import { PageMainContent } from '@/widgets/PageMainContent';
import { memo } from 'react';

export const AdminPanelPage = memo(() => {
	return <PageMainContent data-testid='AdminPanelPage'>ADMINPANELPAGE</PageMainContent>;
});
